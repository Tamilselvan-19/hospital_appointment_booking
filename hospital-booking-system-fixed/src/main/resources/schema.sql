-- ============================================================
-- Hospital Booking System – MySQL Database Setup
-- Run this ONCE to initialize the database
-- (Spring Boot with ddl-auto=update will handle table creation
--  automatically, but this ensures charset and collation)
-- ============================================================

CREATE DATABASE IF NOT EXISTS hospital_booking
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE hospital_booking;
